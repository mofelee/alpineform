<p align="right"><strong>English</strong> | <a href="v0.1.0-alpha.1.zh.md">简体中文</a></p>

## Summary

- This prerelease is incomplete and must not be used. The signed tag and partial
  assets remain published for auditability and are superseded by
  `v0.1.0-alpha.2`.
- First AlpineForm alpha: a reviewable validate, plan, apply, and check loop for
  persistent Alpine 3.24 hosts.
- Real Alpine 3.24.1 x86_64 VM gates cover the complete Beta core.
- Four static CLI archives and checksums were published before the release
  workflow failed.

## Compatibility

- Release phase: alpha; stable compatibility is not promised.
- CLI artifacts: Linux and macOS on amd64 and arm64.
- Beta managed target: Alpine 3.24 x86_64.
- Preview managed target: Alpine 3.24 aarch64; no blocking real-VM gate.
- This is the first release, so there is no prior AlpineForm state migration.

## Breaking Changes

- None relative to a prior AlpineForm release.

## Migration Notes

- None. Back up `/var/lib/alpineform/state.json` before later alpha upgrades.

## Added

- Alpine fact discovery, root SSH, atomic state, renewable leases, deterministic
  plans, native Alpine resources, verified prebuilt components, and deduplicated
  change scripts.
- Blocking no-op, drift, repair, lifecycle, secret, and reboot VM assertions.
- Release automation was added for signed checksums, per-archive SPDX SBOMs,
  GitHub attestations, and the checksum-verifying installer, but the published
  release did not reach those stages.

## Changed

- None.

## Fixed

- None.

## Security

- Protected values are omitted from plan, graph, state, HTML, debug, errors, and
  uploaded integration diagnostics.
- Component artifacts and release archives are checksum verified before use.

## Known Issues

- Alpine 3.24 aarch64 target support is Preview because it lacks a real-VM gate.
- Custom APK signing keys and CA-certificate components have unit/provider
  coverage but are not in the blocking VM fixture.
- Homebrew is not published for this alpha.
- Checksum signing, SBOM generation, provenance, installer verification, and the
  fresh-Alpine verification matrix did not run because the workflow could not
  find the Cosign command.
- Docker (#10), rollback-safe nftables (#13), and target-side source builds
  (#14) remain independent follow-ups outside the v0.1 core.

## Verification

- Commit CI and release dry-run passed.
- The release workflow failed after publishing archives and checksums; all
  supply-chain and installed-binary verification after that point was skipped.

## Verification Matrix

Incomplete; superseded by `v0.1.0-alpha.2`.
