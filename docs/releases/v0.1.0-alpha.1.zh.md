<p align="right"><a href="v0.1.0-alpha.1.md">English</a> | <strong>简体中文</strong></p>

## 摘要

- 此 prerelease 不完整，且不得使用。已签名 tag 和部分 asset 会继续发布以便审计，并由
  `v0.1.0-alpha.2` 取代。
- 首个 AlpineForm alpha：为持久化 Alpine 3.24 host 提供可 review 的 validate、plan、
  apply 和 check 闭环。
- 真实 Alpine 3.24.1 x86_64 VM 门禁覆盖完整 Beta core。
- Release workflow 失败前，已经发布四个静态 CLI archive 和 checksum。

## 兼容性

- Release 阶段：alpha；不承诺 stable 兼容性。
- CLI artifact：Linux 和 macOS，支持 amd64 和 arm64。
- Beta 被管理目标：Alpine 3.24 x86_64。
- Preview 被管理目标：Alpine 3.24 aarch64；没有阻塞式真实 VM 门禁。
- 这是首个 release，因此不存在之前的 AlpineForm state migration。

## 破坏性变更

- 相对于之前的 AlpineForm release，没有破坏性变更。

## 迁移说明

- 无。在后续 alpha 升级前，请备份 `/var/lib/alpineform/state.json`。

## 新增

- Alpine fact discovery、root SSH、原子 state、可续期 lease、确定性 plan、原生 Alpine
  resource、经过验证的预构建 component，以及去重 change script。
- 阻塞式 no-op、drift、repair、lifecycle、secret 和 reboot VM 断言。
- 新增面向已签名 checksum、逐 archive SPDX SBOM、GitHub attestation 和 checksum-verifying
  installer 的 release 自动化，但已发布 release 未执行到这些阶段。

## 变更

- 无。

## 修复

- 无。

## 安全

- Protected value 不会出现在 plan、graph、state、HTML、debug、error 和上传的 integration
  diagnostics 中。
- Component artifact 和 release archive 在使用前均验证 checksum。

## 已知问题

- Alpine 3.24 aarch64 目标支持为 Preview，因为缺少真实 VM 门禁。
- 自定义 APK signing key 和 CA-certificate component 具有 unit/provider 覆盖，但未进入
  阻塞式 VM fixture。
- 此 alpha 不发布 Homebrew。
- 由于 workflow 找不到 Cosign command，checksum signing、SBOM generation、provenance、
  installer verification 和 fresh-Alpine verification matrix 均未运行。
- Docker（#10）、具备回滚安全性的 nftables（#13）和目标侧 source build（#14）仍是
  v0.1 core 之外的独立 follow-up。

## 验证

- Commit CI 和 release dry-run 已通过。
- Release workflow 在发布 archive 和 checksum 后失败；此后的所有 supply-chain 和
  installed-binary verification 均被跳过。

## 验证矩阵

不完整；已由 `v0.1.0-alpha.2` 取代。
