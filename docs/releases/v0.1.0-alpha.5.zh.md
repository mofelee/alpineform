<p align="right"><a href="v0.1.0-alpha.5.md">English</a> | <strong>简体中文</strong></p>

## 摘要

- 首个完整 AlpineForm alpha：为持久化 Alpine 3.24 host 提供可 review 的 validate、plan、
  apply 和 check 闭环。
- 真实 Alpine 3.24.1 x86_64 VM 门禁覆盖完整 Beta core。
- 四个静态 CLI archive 随 checksum、SBOM、Sigstore verification、GitHub provenance 和
  checksum-verifying installer 一起发布。

## 兼容性

- Release 阶段：alpha；不承诺 stable 兼容性。
- CLI artifact：Linux 和 macOS，支持 amd64 和 arm64。
- Beta 被管理目标：Alpine 3.24 x86_64。
- Preview 被管理目标：Alpine 3.24 aarch64；没有阻塞式真实 VM 门禁。
- 此 release 取代不完整的 alpha.1 至 alpha.4 prerelease。不涉及 AlpineForm state
  migration。

## 破坏性变更

- 相对于 alpha.4 code 或 state format，没有破坏性变更。

## 迁移说明

- 不要使用 alpha.1 至 alpha.4 release artifact。请直接安装 alpha.5。
- 在后续 alpha 升级前，请备份 `/var/lib/alpineform/state.json`。

## 新增

- Alpine fact discovery、root SSH、原子 state、可续期 lease、确定性 plan、原生 Alpine
  resource、经过验证的预构建 component，以及去重 change script。
- 阻塞式 no-op、drift、repair、lifecycle、secret 和 reboot VM 断言。
- 已签名 release checksum、逐 archive SPDX SBOM、GitHub attestation，以及
  checksum-verifying installer。

## 变更

- 在构建 snapshot 或发布前检查 release eligibility。
- 在发布 release matrix 前，递归发现 verification result，并将其解析为固定的 allowed
  record 集合。

## 修复

- 使用 release workflow 调用的 command name 安装 Cosign。
- 使用架构专用的 macOS result filename，使合并后的 artifact 保留两个 installer result。
- Aggregate 嵌套的 verification artifact，同时不执行 artifact 提供的 shell content，并在
  diagnostics 中显示任何失败 matrix。
- 保留失败的 prerelease，不移动 tag 或替换部分 asset，然后以新版本发布修正。

## 安全

- Protected value 不会出现在 plan、graph、state、HTML、debug、error 和上传的 integration
  diagnostics 中。
- Component artifact 和 release archive 在使用前均验证 checksum。

## 已知问题

- Alpine 3.24 aarch64 目标支持为 Preview，因为缺少真实 VM 门禁。
- 自定义 APK signing key 和 CA-certificate component 具有 unit/provider 覆盖，但未进入
  阻塞式 VM fixture。
- 此 alpha 不发布 Homebrew。
- Docker（#10）、具备回滚安全性的 nftables（#13）和目标侧 source build（#14）仍是
  v0.1 core 之外的独立 follow-up。

## 验证

- Tag target `1bc7ef803189e3cf773e0a6e8abb551f5968f534` 在创建 signed tag 前已通过
  [CI](https://github.com/mofelee/alpineform/actions/runs/29269315415) 和
  [release dry-run](https://github.com/mofelee/alpineform/actions/runs/29269315360)。
- [Release workflow](https://github.com/mofelee/alpineform/actions/runs/29269741551)
  验证了 asset、checksum、SBOM、Sigstore bundle、provenance、installer metadata，以及在
  全新 Alpine 3.24.1 VM 上运行的已推广 quickstart。
- 已完成的 prerelease 发布为
  [`v0.1.0-alpha.5`](https://github.com/mofelee/alpineform/releases/tag/v0.1.0-alpha.5)。

## 验证矩阵

| 验证项 | linux/amd64 | linux/arm64 | darwin/amd64 | darwin/arm64 |
| --- | --- | --- | --- | --- |
| Artifact build | yes | yes | yes | yes |
| Installer | yes | build-only | yes | yes |
| Supply chain | yes | yes | yes | yes |
| Alpine 3.24 x86_64 quickstart | yes | n/a | n/a | n/a |
