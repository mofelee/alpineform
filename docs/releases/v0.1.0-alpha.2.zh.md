<p align="right"><a href="v0.1.0-alpha.2.md">English</a> | <strong>简体中文</strong></p>

## 摘要

- 此 prerelease 不完整，且不得使用。已签名 tag 和部分 asset 会继续发布以便审计。
- AlpineForm 为持久化 Alpine 3.24 host 提供可 review 的 validate、plan、apply 和 check
  闭环。
- 真实 Alpine 3.24.1 x86_64 VM 门禁覆盖完整 Beta core。
- 在 GitHub 拒绝持久化 provenance 前，已经发布四个静态 CLI archive、checksum、一个
  Sigstore checksum bundle 和四个 SPDX SBOM。

## 兼容性

- Release 阶段：alpha；不承诺 stable 兼容性。
- CLI artifact：Linux 和 macOS，支持 amd64 和 arm64。
- Beta 被管理目标：Alpine 3.24 x86_64。
- Preview 被管理目标：Alpine 3.24 aarch64；没有阻塞式真实 VM 门禁。
- 此 release 取代不完整的 `v0.1.0-alpha.1` prerelease。不涉及 AlpineForm state
  migration。

## 破坏性变更

- 相对于 alpha.1 code 或 state format，没有破坏性变更。

## 迁移说明

- 不要使用 alpha.1 release artifact。请直接安装 alpha.2。
- 在后续 alpha 升级前，请备份 `/var/lib/alpineform/state.json`。

## 新增

- Alpine fact discovery、root SSH、原子 state、可续期 lease、确定性 plan、原生 Alpine
  resource、经过验证的预构建 component，以及去重 change script。
- 阻塞式 no-op、drift、repair、lifecycle、secret 和 reboot VM 断言。
- 已签名 release checksum、逐 archive SPDX SBOM，以及 checksum-verifying installer。
  GitHub attestation 未完成。

## 变更

- Alpha.1 和 alpha.2 的 tag 与部分 asset 只作为可审计的失败 release 保留。

## 修复

- 使用 release workflow 调用的 command name 安装 Cosign，使 signing 和所有后续验证都能
  运行。

## 安全

- Protected value 不会出现在 plan、graph、state、HTML、debug、error 和上传的 integration
  diagnostics 中。
- Component artifact 和 release archive 在使用前均验证 checksum。

## 已知问题

- Alpine 3.24 aarch64 目标支持为 Preview，因为缺少真实 VM 门禁。
- 自定义 APK signing key 和 CA-certificate component 具有 unit/provider 覆盖，但未进入
  阻塞式 VM fixture。
- 此 alpha 不发布 Homebrew。
- 用户所有的私有仓库不支持 GitHub artifact attestation。Provenance persistence 失败后，
  后续 Linux/macOS installer 和 fresh-Alpine verification job 被跳过。
- Docker（#10）、具备回滚安全性的 nftables（#13）和目标侧 source build（#14）仍是
  v0.1 core 之外的独立 follow-up。

## 验证

- Commit CI 和 release dry-run 已通过。
- Release workflow 验证了 artifact creation、checksum signing 和 SBOM generation，随后在
  持久化 GitHub provenance 时失败。Installed-binary 和 fresh-Alpine verification 未运行。

## 验证矩阵

不完整；未生成成功的 verification matrix。
